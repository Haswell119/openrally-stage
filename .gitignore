# Environnements Python
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Caches outils
.pytest_cache/
.mypy_cache/
.ruff_cache/
.install.log

# Géodonnées téléchargées — NE JAMAIS COMMITTER (ancré à la racine)
/data/
*.tif
*.tiff
cache/
osmnx_cache/

# Traces GPX (roadbook / rally-maps) — contenu tiers, NE PAS REDISTRIBUER
*.gpx

# Sorties générées (bundles, mesh, previews) — NE JAMAIS COMMITTER (ancré racine)
/outputs/

# Formats 3D / éditeur lourds
*.fbx
*.kn5
*.obj
*.ply

# Secrets
.env
.env.*
*.secret
secrets/

# OS / éditeurs
.DS_Store
Thumbs.db
.idea/
.vscode/

# Scratch recherche (ne pas committer)
.research_briefs.json

# EXCEPTION : exemple/preview PUBLIC committé (piste démo OSM + swissALTI3D,
# redistribuable AVEC attribution ; voir examples/.../ATTRIBUTION.txt). Les GPX
# rally-maps restent exclus partout.
!examples/
!examples/**
examples/**/*.gpx
